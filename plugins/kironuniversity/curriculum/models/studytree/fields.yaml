# ===================================
#  Form Field Definitions
# ===================================

fields:
  denomination:
    label: Name

  parent:
    label: Parent ID
    type: relation
    nameFrom: path
    emptyOption: No Parent

  take_first:
    label: "Guidance: For how many modules should the first course be recommended"
    type: number
